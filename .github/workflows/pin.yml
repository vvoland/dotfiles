---
name: Action Pin Diff
on:
  pull_request:
    paths:
      - '.github/workflows/**'
      - 'neovim/**'

permissions:
  contents: read
  pull-requests: write

jobs:
  pin-diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: vvoland/gha-pin-diff@v1
